# pakon.conf — USB IDs for the Pakon SANE backend.
#
# Warm (post-firmware) identities — the PID encodes the model. The cold FX2
# bootloader ID is intentionally absent until confirmed on real hardware
# (Phase 1, STOP POINT A).
usb 0x0f05 0xf135   # F-135
usb 0x0f05 0xf235   # F-235 (verified hardware)
usb 0x0f05 0xf335   # F-335
