# pakon.conf — USB IDs for the Pakon SANE backend.
#
# Warm (post-firmware) identity. The cold FX2 bootloader ID is intentionally
# absent until confirmed on real hardware (Phase 1, STOP POINT A).
usb 0x0f05 0xf135
