Current problems:

* We need to migrate the python logic so that user can preview images on the app
* Scanner inits take some time and if you don't load the film super quickly then it finishes the scan. Need more time buffer.
