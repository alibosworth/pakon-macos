# build output
/build/
*.o
*.a

# cmake in-source artifacts (prefer cmake -S . -B build)
CMakeCache.txt
CMakeFiles/
CTestTestfile.cmake
cmake_install.cmake
Makefile

# editor / OS cruft
.DS_Store
*.swp

# local Claude Code state, but track shared project skills
.claude/*
!.claude/skills/

# decoded scan output (large; regenerate with pakon_image.py)
frame_*.tif
frame_*.png
*.raw

# python cruft
__pycache__/
*.pyc

# third-party OEM software + reverse-engineering workspace (copyrighted; not ours
# to redistribute). Findings are folded into docs/PROTOCOL.md instead.
/pakon-scanning-software/
/re/

# local python venv for the image tools
/.venv/
scratch/
